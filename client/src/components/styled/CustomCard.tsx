import { Stack, styled } from '@mui/material';

export const CustomCard = styled(Stack)`
    border-radius: 4px;
    padding: 4px;
    &:hover {
        box-shadow: 0 0.125rem 0.25rem lightgrey;
    }
`;
